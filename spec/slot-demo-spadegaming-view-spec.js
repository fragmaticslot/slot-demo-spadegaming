'use babel';

import SlotDemoSpadegamingView from '../lib/slot-demo-spadegaming-view';

describe('SlotDemoSpadegamingView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
